---

build:
  stage: docs
  image: python:3
  before_script:
    - pip install sphinx
    - pip install sphinx-rtd-theme
    - pip install recommonmark
  script:
    - cd doc ; make html
    - mv build/html/ ../public/
